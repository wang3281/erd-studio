# 보안 정책

[English](SECURITY.md) | **한국어**

> 번역과 영문 원문 사이에 차이가 있으면 [영문 보안 정책](SECURITY.md)을 기준으로 합니다.

## 지원 버전

ERD Studio는 1.0 이전 소프트웨어입니다. 가장 최근에 공개된 `0.1.x` 알파 버전에만 보안 수정이 제공됩니다.

## 취약점 신고

이 저장소의 GitHub 비공개 취약점 신고 기능을 사용해 주세요. 해당 기능을 사용할 수 없다면 `@wang3281` GitHub 프로필에 표시된 비공개 연락 수단으로 저장소 소유자에게 연락하세요. 공개 이슈에는 인증정보, 비공개 ERD 스키마, 운영 데이터베이스 파일 또는 AI 프롬프트를 포함하지 마세요.

완전한 신고는 영업일 기준 5일 이내에 확인하는 것을 목표로 합니다. 영향을 받는 커밋 또는 버전, 재현 조건, 예상 영향 및 실제 데이터가 포함되지 않은 최소 proof of concept를 제공해 주세요.

## 보안 기본값

- `PROJECT_READ_ACCESS=public`을 명시적으로 설정하지 않으면 프로젝트 목록과 스키마 읽기는 비공개입니다.
- 편집자와 관리자 비밀번호는 동일 출처 로그인에서만 교환됩니다. 로그인에 성공하면 불투명하고 해시되며 만료되는 서버 측 세션과 HttpOnly `SameSite=Strict` 쿠키가 생성됩니다.
- 운영 환경에는 HTTPS `APP_BASE_URL`과 `COOKIE_SECURE=true`가 필요합니다.
- OAuth 세션과 편집자 세션은 서로 다른 쿠키를 사용합니다.
- Codex provider 모드는 loopback 전용입니다.

ERD 스키마와 AI 프롬프트는 민감 데이터로 취급하세요. 신뢰할 수 있는 reverse proxy에서 TLS를 종료하고, SQLite 파일과 환경 파일은 서비스 계정만 접근할 수 있도록 제한하세요. 공유 비밀번호를 주기적으로 변경하고 SQLite의 온라인 백업 기능을 사용하세요.

## 알려진 알파 제한사항

- 공유 편집자·관리자 비밀번호는 인스턴스 전체에 적용됩니다. 이번 릴리스에는 사용자별 프로젝트 ACL이나 편집 작업의 감사 주체 기록이 없습니다.
- 지원 구성은 Fastify 프로세스 1개와 SQLite 데이터베이스 1개입니다. 수평 확장에는 외부 세션·데이터 저장소가 필요하며 현재 지원하지 않습니다.
- 로컬·오프라인 초안은 JSON으로 내보내기 전까지 브라우저 메모리에 남습니다.
- 릴리스 lockfile은 Vite 7.3.6과 패치된 `esbuild@0.28.1`을 사용합니다. 개발 서버는 로컬 도구이므로 신뢰할 수 없는 네트워크에 공개하지 마세요. 릴리스 CI는 전체 의존성 트리를 검사합니다.
